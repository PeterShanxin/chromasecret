{
  "name": "chromasecret", "version": "0.1.0", "private": true,
  "description": "Local-only experimental differential color-visibility laboratory",
  "scripts": { "dev": "node scripts/serve.cjs", "start": "node scripts/serve.cjs", "build": "node scripts/build.cjs", "test": "node --test tests/*.test.cjs", "experiment": "node scripts/synthetic.cjs" },
  "devDependencies": { "typescript": "5.9.3" },
  "engines": { "node": ">=20" }
}
